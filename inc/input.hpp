
#ifndef __INPUT_CONTEXT_HPP_INCLUDED__
#define __INPUT_CONTEXT_HPP_INCLUDED__


/*


    WEAPON_FIRE_KEY
    PLAYER_MOVE_KEY



    Press scroll to flip holding entity.
    Press space to flip whole entity family.


    Excavator
        
        Boom, stick and bucket will move to mouse position.







    Flipping creatures (and maybe mobile crane) are done with moving the mouse.
    Flipping other vehicles are done with space?



*/
/*
enum Action
{
    FLIP, // Space
    JUMP, // W
};

enum State
{
    WALK
};

enum Range
{

};



struct InputContext
{

    std::unordered_map<RawInputButton, Action> actionMap;
    std::unordered_map<RawInputButton, State> stateMap;
    std::unordered_map<RawInputAxis, range> rangeMap;


};
*/




#endif